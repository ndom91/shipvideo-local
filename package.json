{
  "name": "shipvideo-local",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "setup-local": "npm install && npx playwright-core install chromium-headless-shell"
  },
  "dependencies": {
    "ffmpeg-static": "^5.2.0",
    "playwright-core": "^1.57.0"
  },
  "engines": { "node": ">=22.0.0" }
}
